---
# Proxy Agent handlers

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true

- name: Restart proxy-agent
  ansible.builtin.systemd:
    name: "{{ proxy_agent_service_name }}"
    state: restarted
  become: true
